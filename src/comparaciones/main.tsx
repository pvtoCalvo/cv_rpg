import React from "react";
import ReactDOM from "react-dom/client";
import ComparacionesApp from "./ComparacionesApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ComparacionesApp />
  </React.StrictMode>
);
